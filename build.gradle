buildscript {
	repositories {
		mavenLocal()
		mavenCentral()
	}
	dependencies {
		classpath group: 'org.gradlefx', name: 'gradlefx', version: '0.6'
	}
}

defaultTasks 'clean', 'test', 'compile', 'package', 'war'
